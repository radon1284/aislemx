<%= form_for(@store) do |f| %>
  <% if @store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store.errors.count, "error") %> prohibited this store from being saved:</h2>

      <ul>
      <% @store.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :store_name %>
    <%= f.text_field :store_name, class: "form-control", placeholder: :store_name %>
  </div>
  <div class="form-group">
    <%= f.label :store_image %>
    <%= f.text_field :store_image, class: "form-control", placeholder: :store_image %>
  </div>
  <div class="form-group">
    <%= f.label :store_location %>
    <%= f.text_area :store_location, class: "form-control" , placeholder: :store_location %>
  </div>

    <%= f.submit 'Add Store', class: "btn btn-primary" %>
<% end %>