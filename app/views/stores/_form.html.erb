<%= form_for(@store) do |f| %>
  <% if @store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store.errors.count, "error") %> prohibited this store from being saved:</h2>

      <ul>
      <% @store.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :store_name %><br>
    <%= f.text_field :store_name %>
  </div>
  <div class="field">
    <%= f.label :store_image %><br>
    <%= f.text_field :store_image %>
  </div>
  <div class="field">
    <%= f.label :store_location %><br>
    <%= f.text_area :store_location %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
